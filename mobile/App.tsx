import AppNavigator from './src/navigation/AppNavigator';

export default function App() {
  return <AppNavigator />;
}
/*App.tsx es el punto de entrada de toda la app,
 es el archivo principal que se ejecuta al iniciar la aplicación */
